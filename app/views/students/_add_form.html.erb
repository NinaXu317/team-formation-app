<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_with(url: search_course_student_path, method: "post") do %>
             <%= label_tag(:label, "Course Pin") %>
             <%= text_field_tag(:pin) %>
             <%= submit_tag("Add") %>
        <% end %>
    </div>
</div>