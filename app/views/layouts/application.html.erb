<!DOCTYPE html>
<html>
  <head>
    <title>TeamFormation</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/4f7943abdb.js" crossorigin="anonymous"></script>
    
  </head>
  <body>
    <header class="navbar navbar-expand-lg navbar-dark navbar-custom">
      <div class="container">
        <%= link_to "TeamFormation", root_path, id: "logo", class: "navbar-brand"%>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <% if !logged_in? %>
              <li><%= link_to "Student Sign Up", new_student_path, class: "nav-link" %></li>
              <li><%= link_to "Professor Sign Up", new_professor_path, class: "nav-link" %></li>
            <% else %>
              <li><%= link_to "Profile", profile_url, class: "nav-link" %></li>
              <li><%= link_to "Logout", logout_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
    <%= yield %>
    <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
