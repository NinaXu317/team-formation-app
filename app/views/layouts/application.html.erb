<!DOCTYPE html>
<html>
  <head>
    <title>Teamation</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/4f7943abdb.js" crossorigin="anonymous"></script>
    
  </head>
  <body>
    <header class="navbar navbar-expand-lg navbar-dark navbar-custom">
      <div class="container">
        <a href="/"> <img src="/assets/logo.png" height="100" width="130"> </a>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <% if !logged_in? %>
              <li><%= link_to "Student Sign Up", new_student_path, class: "nav-link" %></li>
              <li><%= link_to "Professor Sign Up", new_professor_path, class: "nav-link" %></li>
            <% else %>
              <% if current_user_type == "professor"%>
                <li><%= link_to "Professor Home", profile_url, class: "nav-link" %></li>
              <% elsif current_user_type == "student" %>
                <li><%= link_to "Student Home", profile_url, class: "nav-link" %></li>
              <% end %>
              
              <li><%= link_to "Logout", logout_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
    <%= yield %>
    <div class="container">
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
